<%= render 'user/user_header' %>

<h2>マイページ</h2>

<table>
  <tr>
    <th>名前</th>
      <td><%= @user.name %></td>
  </tr>
  <tr>
    <th>イントロダクション</th>
      <td><%= @user.introduction %></td>
  </tr>
</table>

<%= link_to "登録内容変更",edit_user_path(@user) %>

<%= link_to "新規楽曲投稿",new_music_path %>